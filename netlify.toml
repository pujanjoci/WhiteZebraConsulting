# Temporary static deployment (no build)
# To enable automatic builds, uncomment the lines below after debugging

# [build]
#   command = "npm run build:blog"
#   publish = "."

[build]
  publish = "."

[[redirects]]
  from = "/src/blog-post.html"
  to = "/src/:slug.html"
  status = 301
  force = false
